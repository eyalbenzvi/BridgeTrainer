# DECISIONS.md — מודול "ניתוח הכרזה" (Bidding Analysis)

מסמך זה מרכז את כל ההחלטות שהתקבלו עצמאית במהלך פיתוח המודול, כנדרש
בהנחיות העבודה. שפת הקוד וההערות: אנגלית (כמו שאר הריפו); מסמך זה
בעברית כי הוא פונה למשתמש.

---

## 1. סריקת הריפו — רכיבים קיימים ושימוש חוזר

הריפו הקיים מכיל תשתית עשירה שנבנתה עבור מכין-הבעיות. טבלת ההחלטות:

| רכיב קיים | מיקום | החלטה | נימוק |
|---|---|---|---|
| ייצוג יד/חפיסה וקטורי | `dealing/features.py` (`parse_hand_pbn`, `hand_to_pbn`, `SeatFeatures`) | **שימוש חוזר מלא** | בדוק, וקטורי, עם טסט צ'י-בריבוע (INV3) |
| מנוע אילוצים רכים | `domain/constraints.py` (`SeatConstraints`, `Band`, `Denial`, `HonorSpec`) | **שימוש חוזר מלא** | בדיוק הפרימיטיבים הנדרשים: פסים משוקללים, שלילות ("מי שעבר לא מחזיק X"), מיזוג-כפל של אילוצים |
| דגימת דחייה וקטורית | `dealing/rejection.py` (`RejectionDealSource`) | **שימוש חוזר מלא** | כבר ממומש short-circuit לפי עלות (HCP קודם, אורכים אח"כ, פרדיקטים בסוף) — עונה על דרישת "לחלק מוגבלות תחילה" ברמת האפקט: חלוקות נפסלות מוקדם וזול. תקרת ניסיונות/זמן קיימת (`GenerationBudget`) |
| פותר דאבל-דאמי | `dd/solver.py` (עטיפת endplay/DDS של Bo Haglund, chunked) | **שימוש חוזר מלא** | הדרישה במפורש: לא לממש DDS מאפס |
| כיול DD→Single-Dummy | `dd/correction.py` + `correction_table.yaml` | **שימוש חוזר מלא** | טבלה מבוססת-ספרות, נערכת במקום אחד, מוצגת סימטרית (INV5) — בדיוק סעיף 3.3 |
| טבלאות ניקוד ו-IMP | `scoring/tables.py` (`contract_score`, `imps_array`) | **שימוש חוזר מלא** | golden-tested מול endplay |
| סטטיסטיקה משוקללת + רווחי סמך | `scoring/stats.py` (`weighted_ci`, ESS) | **שימוש חוזר מלא** | INV2/INV7 — רווחי סמך אמיתיים דרך effective sample size |
| השוואת מועמדים זוגית | `scoring/comparison.py` (`compare_candidates`) | **שימוש חוזר מלא** | INV1 — אותו סט חלוקות לכל המועמדים; toss-up כשההפרש בתוך ה-CI |
| ייצוג מכרז | `domain/auction.py` (`Auction`, `Call`, seats) | **שימוש חוזר מלא** | |
| ייצוג חוזה סופי | `domain/contracts.py` (`FinalContract`) | **שימוש חוזר מלא** | |
| פיצ'רים לחוזים-מותנים | `projection/tree.py` (`deal_features`) | **שימוש חוזר** של `deal_features` בלבד | ראו החלטה 2.3 למה לא העצים המחוברים |
| מערכת עיצוב, RTL, גליפי סדרות | `app/webapp.py` (`_CSS` tokens), `app/htmlfmt.py` | **שימוש חוזר** בטוקנים (צבעים, קלפים, טבלת מכרז) בעמוד החדש | האפליקציה כולה עברית-RTL על "לבד ירוק"; העמוד החדש שומר על אותה שפה עיצובית |
| מנוע סמנטיקה קיים | `semantics/engine.py` + `semantics/rules/*.yaml` | **שימוש חוזר חלקי**: הפורמט של אילוצי-חוקים (`rule_to_constraints`, מבנה core/margin) נשמר; האינדוקס **לא** | ראו החלטה 2.1 |
| מחסן/DB | Firestore (`pool/`) + localStorage | **לא בשימוש בשלב זה** | ניתוח הוא חישוב לוקאלי per-request; אין צורך בפרסיסטנטיות בגרסה 1. הדוח נשמר כקובץ HTML/PDF/JSON בדיסק (`reports/analysis/`) |
| מנוע Ben | `engine/ben.py` | **לא בשימוש** | דורש checkout חיצוני + GPU-ישחק; המשימה דורשת חישוב דטרמיניסטי זול. הפרויקציה ההיוריסטית + DD מספקת את העומק בעלות אפס |
| מכונת-מצבים לחוקיות מכרז | `validate/auction_state.py` (`AuctionState`, `replay`) | **שימוש חוזר מלא** | חוקיות הכרזות, זיהוי סיום, וקביעת הכרוז (first namer) — גם בוולידציית הקלט וגם כבסיס לולאת ההמשכים; שוכפלה ל-JS בעמוד ה-UI לנטרול כפתורים לא-חוקיים |

## 2. החלטות ארכיטקטורה

### 2.1 מנוע אילוצים חדש-מוכלל (`analysis/systems/`) ולא `semantics/engine.py`

המנוע הקיים ממפתח חוקים לפי **הרצף המדויק** של הכרזות קודמות
(`context: ["1S","P","2S"]`) — מצוין לבעיות מחוברות-מראש, בלתי-ישים
למכרז חופשי שמזין משתמש (שילובים אינסופיים). לכן:

- נכתב אינטרפרטר מצב-מכרז גנרי (`analysis/systems/interpreter.py`):
  לכל הכרזה נגזר **תפקיד** (פותח/עונה/מתערב/מסייע/פס) מתוך מצב
  המכרז, והתפקיד+ההכרזה ממופים לערך מוסכם בטבלת השיטה.
- פרמטרי השיטות (טווחי נק', אורכים, איכות) יושבים ב-YAML לכל שיטה
  (`sayc.yaml`, `two_over_one.yaml`) — **טבלת חוקים/DSL נפרדת**, כפי
  שנדרש בסעיף 3.1, בפורמט core/margin **זהה** לזה של
  `semantics/rules` כדי לא להמציא DSL שני.
- הפלט הוא `SeatConstraints`/`ConstraintProfile` הקיימים — כך כל
  שרשרת הדגימה/שקילה נשארת ללא שינוי.
- Pass הוא חוק מן המניין (INV8 נשמר): פס-בפתיחה קובע תקרת נק',
  פס מעל התערבות מוסיף שלילות (למשל "לא צורת דאבל-נגד").
- רצף לא מכוסה: ברירת מחדל מתועדת לפי גובה ההכרזה (ראו
  `interpreter.py::_fallback_meaning`), וכל שימוש בברירת מחדל נאסף
  ל-`transparency_notes` שמופיעות בדוח ("ההנחה על X הייתה...").

### 2.2 דריסות משתמש ("הוספת משמעויות")

דריסה = אובייקט מובנה לכל אינדקס-הכרזה: `{hcp: [lo,hi],
suits: {S:[lo,hi],...}, note: "טקסט חופשי"}`. השדות המובנים נהפכים
ל-`SeatConstraints` **שמחליף** את פרשנות-השיטה לאותה הכרזה (לא
מצטרף אליה — המשתמש דורס). הטקסט החופשי אינו משפיע על הסימולציה
(אין LLM בשכבת החישוב!) אך מוצג בדוח כהערת שקיפות. זה שומר על
הכלל "כל מסקנה מספרית נובעת מחישוב קוד".

### 2.3 מנוע המשכים חדש (`analysis/continuation.py`) ולא עצי YAML

העצים הקיימים (`projection/tree.py`) נכתבים ידנית לכל בעיה — אין
מחבר אנושי בזרימת המשתמש החדשה. לכן נבנה מקרן-חוזה היוריסטי
פרמטרי: לכל חלוקה ולכל פעולה מועמדת הוא מסיק את החוזה הסופי לפי
היוריסטיקות ברידג' (הערכת סך-נקודות משולב, התאמת שליט, תחרות,
הקרבה, והכפלת עונשין עם סטאק בשליט בגובה 4+). שלוש מדיניות:
`conservative` / `realistic` / `omniscient` (חסם עליון "רואה
קלפים" — הצד המחליט בוחר את התוצאה הטובה לו ב-DD מבין המשכיו
החוקיים). **כל** הפרמטרים המספריים ב-`analysis/policies.yaml` אחד —
לא hardcoded מפוזר. `deal_features` הקיים משמש כשכבת הפיצ'רים.

### 2.4 דגימה אדפטיבית

בלוקים של 250 חלוקות; אחרי כל בלוק נבדק אם רווח הסמך (95%,
משוקלל-ESS) של הפרש ה-IMP בין שתי הפעולות המובילות אינו חוצה אפס,
או שההפרש קטן מ-0.25 IMP (אז ההבחנה חסרת משמעות מעשית — עוד דגימות
לא ישנו את ההמלצה). תקרה: **2,000 חלוקות** (בתוך הטווח שהומלץ
1500–2500; ב-13ms לחלוקה ל-2 זנים זה ≈50s גג במקרה הגרוע, ובפועל
רוב הניתוחים נעצרים מוקדם). גודל המדגם בפועל ורווח הסמך מודפסים
בדוח (סעיף "סייגים").

### 2.5 IMP מול Matchpoints

אותה סימולציה, שני מנועי סיכום: IMP — תוחלת מול הפעולה החלופית
הטובה ביותר (התשתית הקיימת). MP — לכל חלוקה מדורגת כל פעולה מול
"שדה" שמוגדר כאוסף התוצאות של כל הפעולות המועמדות על אותה חלוקה
(קירוב שדה מקובל כשאין נתוני שדה אמיתיים; מתועד בדוח), וההמלצה
מנוסחת לפי אחוז-הניצחון הממוצע ולא לפי תוחלת נקודות. שני המצבים
נשמרו בגרסה 1 (סדר הוויתורים לא הופעל — לא נדרש צמצום).

### 2.6 שכבת ניסוח

- ברירת מחדל: **מנסח תבניות בעברית** (`analysis/narrate.py`) —
  עלות אפס, דטרמיניסטי, מלא לפי מבנה 4.1.
- אופציונלי: קריאת LLM בודדת (`analysis/llm_narrator.py`) עם
  system prompt קבוע (הנחיות מבנה/סגנון/דוגמה) מסומן
  `cache_control` ל-prompt caching, וה-JSON המחושב בלבד כתוכן
  משתנה. מודל: `claude-haiku-4-5` (הזול ביותר); ללא extended
  thinking. מופעל רק כש-`ANTHROPIC_API_KEY` קיים והמשתמש ביקש
  (`narration: "llm"`); כל כשל → fallback שקוף לתבניות.
- בסביבת פיתוח זו אין מפתח API, לכן הערכת העלות בפועל (סעיף 4.3)
  מבוססת על ספירת טוקנים מקומית של הפרומפט שנבנה בפועל — ראו סעיף 5.

### 2.7 ממשק ושרת

האפליקציה הקיימת היא אתר סטטי + Firestore — אין לה שרת חישוב, ואי
אפשר להריץ DDS/סימולציה בדפדפן. לכן: `trainer analyze serve` מרים
שרת HTTP לוקאלי (stdlib, ללא תלות חדשה) שמגיש את עמוד הניתוח
(עברית, RTL, אותה מערכת עיצוב) ומחשיב ב-POST ‎/api/analyze. עלות
תפעול שוטפת: אפס (חישוב מקומי). ייצוא PDF: דרך Chromium headless
אם מותקן (`--print-to-pdf`), אחרת עמוד הדוח כולל CSS להדפסה כך
ש"הדפס→PDF" מהדפדפן נותן תוצאה זהה.

### 2.8 בחירת קלפים ויזואלית

לא קיים בורר-קלפים באפליקציה (הבעיות מוכנות מראש), לכן נבנתה
קומפוננטה חדשה בעמוד הניתוח: רשת 52 קלפים בארבע שורות-סדרה,
לחיצה לבחירה/ביטול, מונה נותר לכל סדרה ובסה"כ, חסימת קלף כפול
מובנית (לחיצה שנייה מבטלת). לצידה "הזנה מהירה" טקסטואלית
בפורמט PBN‏ (`AKQ2.T98.Q54.J32`) שממלאת את הבורר — קיצור דרך,
לא תחליף. תיבת ההכרזות היא רשת 35 הכרזות + פס/דאבל/רידאבל עם
נטרול הכרזות לא-חוקיות לפי מצב המכרז וזיהוי אוטומטי של סיומו.

## 3. החלטות ברידג' מהותיות

- **תפקידי מכרז**: הזיהוי (פותח/עונה/מתערב/מסייע) נגזר מסדר
  הפעולות בפועל, לא ממושבים קבועים.
- **פס בפתיחה**: תקרה 12 נק' core עם שוליים 13–14 במשקל 0.3
  (יד "טראפ"/לא נוחה) — עקבי עם `pass_over_1h` הקיים.
- **הכפלת עונשין באנסמבל**: בכל המדיניות (גם השמרנית) יריב שמחזיק
  4+ קלפי שליט בחוזה מוכרז בגובה 4+ עם ערכי הגנה מעל סף (מוגדר
  ב-`policies.yaml`) מכפיל — הלקח מניתוח ההכפל שצוטט במשימה.
- **מסקנה יציבה**: פעולה שמנצחת תחת כל המדיניות מסומנת "מסקנה
  יציבה"; אם הדירוג מתהפך בין מדיניות, הדוח מציין במפורש איזו
  הנחה (שם המדיניות והפרמטר) הופכת אותו.
- **חלוקות מייצגות**: לפעולה המומלצת נשלפות טיפוסית (קרובה לחציון
  ההפרש מול החלופה), כישלון אופייני (אחוזון ~10) ואסון (המינימום,
  אם הפסדו ≥5 IMP), בתצוגת ארבע ידיים + תוצאת DD.

## 4. סטטוס ביצוע מול סדר העדיפויות (סעיף 5 במשימה)

לא הופעל אף ויתור: Matchpoints ממומש, שלוש מדיניות ממומשות,
ושתי השיטות (SAYC ו-2/1) ממומשות. הליבה שאין לוותר עליה — אילוצי
Pass, דגימה אדפטיבית עם CI, כיול DD→SD, חלוקות מייצגות, ממשק
ויזואלי — ממומשת במלואה.

## 5. הערכת עלות בפועל לניתוח בודד

נמדד על תרחיש הקבלה (מנע 3♥, החלטת ‎3NT/X/P, ‏500 חלוקות בעצירה
אדפטיבית, seed 2026):

- **חישוב מקומי**: ‏33 שניות CPU, עלות ענן 0.
- **מצב תבניות (ברירת המחדל)**: 0 טוקנים, עלות 0 — הדוח המצורף הופק כך.
- **מצב LLM** (בסביבת הפיתוח אין `ANTHROPIC_API_KEY`, ולכן המדידה היא
  ספירה מקומית של הפרומפט שנבנה בפועל, לא הערכה תיאורטית של תוכן):
  - פרומפט סטטי (הנחיות מבנה/סגנון, מסומן ל-prompt caching): ‏1,355
    תווים ≈ ‏340–450 טוקנים. לאחר הקריאה הראשונה נקרא מהמטמון
    (עלות קריאה ≈ 10% מהמחיר).
  - נתונים משתנים (facts JSON מלא של הניתוח): ‏6,748 תווים ≈
    ‏2,200–2,700 טוקנים.
  - פלט: תקרה קשיחה 3,000 טוקנים; דוח טיפוסי ≈ ‏1,000–1,500.
  - במחירי `claude-haiku-4-5`‏ ($1/$5 למיליון): קלט ≈ $0.003, פלט ≈
    $0.005–0.0075 → **סה"כ ≈ $0.01 לניתוח**, ופחות מכך עם מטמון חם.
  - ללא extended thinking (כמתחייב מהמשימה); קריאה אחת בלבד לדוח.
- אם איכות הניסוח של Haiku לא תספק בשימוש אמיתי — המדרגה הבאה היא
  `claude-sonnet-5` (פי ~3 במחיר, עדיין ~$0.03 לניתוח), לא ישר למודל
  היקר ביותר.

## 7. מסלול ענן: הרצה מהאתר, חישוב ב-GitHub Actions

(נוסף בהמשך לבקשת המשתמש: הפעלה מהטלפון דרך האתר הקיים, ללא התקנות.)

### ארכיטקטורה

```
analyze.html (אתר סטטי, gh-pages, מאחורי חיבור Google הקיים)
   │ כתיבת מסמך בקשה               ▲ עדכון חי (onSnapshot)
   ▼                                │
Firestore: analysis_requests  ──►  analysis_reports
   ▲ תביעת בקשה (CAS) + כתיבת תוצאה
   │
GitHub Actions: analyze-requests.yml (cron כל 5 דק' + הפעלה ידנית)
   └─ trainer analyze-worker  →  אותו מנוע ניתוח בדיוק (analysis/pipeline)
```

- **קלט**: עמוד `analyze.html` באתר, עם אותם רכיבי קלט בדיוק כמו הכלי
  המקומי — הקומפוננטות חולצו לנכס משותף `web/bt-analyze-ui.js` (מימוש
  אחד, שני מסלולי שליחה), משתלב במעטפת האפליקציה הקיימת (עיצוב, ניווט
  תחתון, שער התחברות Google של `bt-firebase.js`).
- **תור**: מסמכי `analysis_requests` ב-Firestore הקיים. חוקי האבטחה
  (firestore.rules) מאשרים למשתמש מחובר ליצור בקשה **בצורה קשיחה בלבד**
  (allowlist שדות, טווחים, uid == המשתמש, status="pending"), לקרוא
  ולמחוק רק את שלו, ולעולם לא לעדכן סטטוס — מעברי הסטטוס שייכים ל-worker
  בלבד (Admin SDK, עוקף rules, כמו כל שאר צד-הייצור בריפו).
- **חישוב**: workflow חדש `analyze-requests.yml` על cron של 5 דקות +
  `workflow_dispatch`. תביעת בקשה בטרנזקציית CAS ‏(pending→running) כך
  שריצות חופפות לא יעבדו אותה בקשה פעמיים; עד 6 בקשות לריצה; כל כשל
  נרשם כ-status="error" עם סיבה שמוצגת למשתמש.
- **תוצאה**: הדוח (HTML מלא ~60–100KB + facts JSON) נכתב ל-
  `analysis_reports/{id}`; העמוד כבר מאזין ב-onSnapshot ומציג "מוכן"
  ואת הדוח בתוך iframe — בלי ריענון ובלי polling מהלקוח.

### החלטות ונימוקים

- **עלות ריצה**: הריפו ציבורי → דקות Actions חינמיות. ובכל זאת, טיק
  ריק זול בכוונה: שער מקדים (`scripts/analysis_queue_gate.py`) מתקין רק
  firebase-admin, סופר בקשות ממתינות, ומדלג על ההתקנה הכבדה
  (endplay/numpy) כשהתור ריק — המצב השכיח.
- **השהיה צפויה**: cron מינימלי של GitHub הוא 5 דקות ולעיתים מתעכב —
  חוויית המשתמש המוצהרת בעמוד היא "בדרך כלל 5–10 דקות". זה המחיר של
  עלות-אפס; שדרוג עתידי לשרת קטן ייתן ~30 שניות במחיר תפעול שוטף.
- **PDF במסלול הענן**: לא נשמר קובץ PDF (אין Storage מוגדר, ומסמכי
  Firestore מוגבלים ל-1MiB) — הדוח כולל CSS הדפסה מלא, וכפתור "פתח
  בחלון מלא" מאפשר הדפסה/שמירה-כ-PDF מהדפדפן בנייד ובמחשב. הכלי
  המקומי ממשיך להפיק PDF אמיתי.
- **לוגים ציבוריים**: הריפו ציבורי ⇒ לוגי ה-workflow ציבוריים. ה-worker
  מדפיס מזהים/זמנים/ספירות בלבד — לעולם לא ידיים, מכרזים או תוכן דוח
  (יש טסט שאוכף זאת). תוכן הבקשות והדוחות חי רק ב-Firestore, מאחורי
  ה-rules.
- **מכסה יומית**: 20 ניתוחים למשתמש ליום (נאכף ב-worker) — הגנה על
  ה-runner ועל מכסת הקריאה החינמית של Firestore מפני הצפה, בהינתן שכל
  חשבון Google יכול להתחבר לאתר.
- **ניסוח במסלול הענן**: תבניות בלבד כרגע (עלות אפס). הפעלת LLM דורשת
  רק הוספת `ANTHROPIC_API_KEY` כ-secret ושליחת `narration: "llm"`
  מהעמוד — הצנרת קיימת.
- **פריסה**: אין צעד ידני מעבר ל-merge. ‏publish.yml הקיים כבר פורס את
  ה-rules המעודכנים ואת העמוד החדש בכל push ל-main; ה-cron החדש מתחיל
  לרוץ עם הופעת הקובץ ב-main. ה-secret ‏FIREBASE_SERVICE_ACCOUNT כבר
  קיים (משמש את forge/publish).

## 8. מסלול מהיר: Cloud Functions ‏(Cloud Run) בפרויקט ה-Firebase הקיים

(נוסף לבקשת המשתמש: תגובה מהירה מהחינם של Google, במקום המתנה ל-cron.)

### מה נוסף

- `functions/main.py` — פונקציית ‎2nd gen‎ (פייתון 3.11, רצה על Cloud
  Run) עם **טריגר Firestore**: נורית אוטומטית ברגע שנוצר מסמך
  `analysis_requests`, תובעת אותו באותה טרנזקציית CAS, מריצה את אותו
  מנוע (`worker.handle_request`) וכותבת את הדוח — **דקה-שתיים** מלחיצת
  "נתח" במקום 5–10 דקות.
- `functions/requirements.txt` — המנוע מותקן היישר מהריפו הציבורי
  (`git+https://...@main`); עדכון מנוע ⇒ הרצה מחודשת של פריסת הפונקציה.
- `firebase.json` + `.firebaserc` — הגדרת ה-codebase לפריסה.
- `deploy-functions.yml` — פריסה בלחיצת כפתור מ-GitHub Actions
  (workflow_dispatch) עם ה-secret הקיים; אין צורך בהתקנות מקומיות.

### חלוקת תפקידים בין שני המסלולים

| | Cloud Function (ראשי) | GitHub Actions cron (גיבוי) |
|---|---|---|
| טריגר | יצירת מסמך (מיידי) | כל 5 דקות |
| זמן תגובה | ~30–90 שניות | 5–10 דקות |
| תפקיד נוסף | — | "שרת ניקיון": מחזיר בקשות שנתקעו ב-running (קריסה) ל-pending ומעבד כל מה שהפונקציה פספסה |
| בטיחות כפילות | תביעת CAS משותפת — שני המסלולים לעולם לא מעבדים אותה בקשה פעמיים | |

לאחר שהפונקציה מוכחת אפשר לרופף את ה-cron ל-‎*/30‎ (שורת cron אחת
ב-analyze-requests.yml).

### עלות וגבולות

- מכסת החינם של Cloud Run‏ (180,000 שניות-vCPU לחודש) ≈ אלפי ניתוחים —
  הרבה מעבר למכסה היומית (20 למשתמש) שכבר נאכפת ב-worker.
- חסמים מובנים: ‏max_instances=2, ‏concurrency=1, ‏timeout 540s,
  ‏1GiB זיכרון; טריגרי Firestore אינם עושים retry — כשל נרשם כ-error
  ולא כריצה אינסופית.
- הדרישה היחידה בתשלום-פוטנציאלי: תוכנית **Blaze** (חיוב מחובר). בהיקף
  אישי החשבון יישאר ‎0₪‎; מומלץ להגדיר התראת תקציב.
- `REGION` בפונקציה חייב להתאים למיקום מסד ה-Firestore ‏(ברירת מחדל
  `us-central1`, המתאים ל-nam5); אם המסד באזור אחר — משנים את הקבוע
  ופורסים מחדש (שגיאת מיקום בפריסה = זה התיקון).
- אימות: הדקורטור אומת מול ה-SDK האמיתי (‏firebase-functions‏) בזמן
  הפיתוח — סוג אירוע, אזור, זיכרון וחסמים נבדקו בפועל; הלוגיקה המשותפת
  מכוסה בטסטים של ה-worker.

## 6. סטטוס קריטריון הקבלה (סעיף 6 במשימה)

תרחיש קצה-לקצה שהורץ: מזרח פותח 3♥ (מנע), הגיבור בדרום עם
‎♠AQ2 ♥KJ3 ♦KQ54 ♣A32‎ (19 נק'), פגיעות שני הצדדים, ‏2/1, IMP;
נקודת ההחלטה: הכרזה #2, מועמדים ‎3NT / X / P. הדוח שהופק
(`reports/analysis/acceptance-3H-decision.html/.pdf/.json`):

- ✔ כל שבעת סעיפי המבנה של 4.1.
- ✔ רווחי סמך אמיתיים (‏±0.71 IMP על מדגם 500; משתנה עם ה-seed).
- ✔ הכפלות עונשין באנסמבל: החוזים ‎3♥X, 4♥X, 4♠X ועוד מופיעים
  בטבלאות התדירות.
- ✔ ארבע חלוקות מייצגות אמיתיות מהסימולציה (חציון / אחוזון 97 /
  אחוזון 10 / הגרוע במדגם), בפריסת ארבע ידיים.
- ✔ יוצא ל-PDF תקין וקריא ב-RTL (Chromium headless; אומת ויזואלית).
- הערת שקיפות מעניינת שהתרחיש חשף: תחת "פס" השותף ממשיך להתחרות
  בחלק מהחלוקות (איזון) — לכן "פס" איננו שקול ל"הגנה על 3♥" בכל
  חלוקה; זו התנהגות מכוונת של מדיניות ההמשך והיא מוסברת בדוח.
