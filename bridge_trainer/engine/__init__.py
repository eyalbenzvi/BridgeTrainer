"""Ben-powered problem generation (docs/ben_execution_plan.md).

Requires the Python 3.12 venv with TensorFlow + the ben checkout
(scripts/setup_ben.sh). Imports are lazy so the rest of bridge_trainer
works without them.
"""
